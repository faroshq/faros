{{- if .Values.agent.hub.token }}
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: {{ include "faros-agent.fullname" . }}-kubeconfig
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "faros-agent.labels" . | nindent 4 }}
rules:
- apiGroups: [""]
  resources: ["secrets"]
  resourceNames: ["faros-agent-{{ .Values.agent.edgeName }}-kubeconfig"]
  verbs: ["get", "update", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "faros-agent.fullname" . }}-kubeconfig
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "faros-agent.labels" . | nindent 4 }}
subjects:
- kind: ServiceAccount
  name: {{ include "faros-agent.serviceAccountName" . }}
  namespace: {{ .Release.Namespace }}
roleRef:
  kind: Role
  name: {{ include "faros-agent.fullname" . }}-kubeconfig
  apiGroup: rbac.authorization.k8s.io
{{- end }}
