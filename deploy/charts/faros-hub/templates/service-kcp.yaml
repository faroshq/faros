{{- if not .Values.kcp.external.enabled }}
{{- $fullname := include "faros-hub.fullname" . }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $fullname }}-kcp
  labels:
    {{- include "faros-hub.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  # The shard URL kcp advertises is this pod's DNS name under this Service
  # (faros-hub.kcpShardURL). The hub dials it while it is still starting,
  # before its readiness probe passes, so the record must exist for a
  # not-ready pod too.
  publishNotReadyAddresses: true
  ports:
    - name: kcp-https
      port: {{ .Values.kcp.embedded.securePort }}
      targetPort: {{ .Values.kcp.embedded.securePort }}
      protocol: TCP
  selector:
    {{- include "faros-hub.selectorLabels" . | nindent 4 }}
{{- end }}
