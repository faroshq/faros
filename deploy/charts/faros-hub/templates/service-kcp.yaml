{{- if not .Values.kcp.external.enabled }}
{{- $fullname := include "faros-hub.fullname" . }}
apiVersion: v1
kind: Service
metadata:
  name: {{ $fullname }}-kcp
  labels:
    {{- include "faros-hub.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: kcp-https
      port: {{ .Values.kcp.embedded.securePort }}
      targetPort: {{ .Values.kcp.embedded.securePort }}
      protocol: TCP
  selector:
    {{- include "faros-hub.selectorLabels" . | nindent 4 }}
{{- end }}
