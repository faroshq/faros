apiVersion: apis.kcp.io/v1alpha1
kind: APIResourceSchema
metadata:
  name: v260912-0c922d1f.connections.linear.providers.faros.sh
spec:
  group: linear.providers.faros.sh
  names:
    kind: Connection
    listKind: ConnectionList
    plural: connections
    singular: connection
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1alpha1
    schema:
      description: Connection is a Linear provider API resource.
      properties:
        apiVersion:
          description: |-
            APIVersion defines the versioned schema of this representation of an object.
            Servers should convert recognized schemas to the latest internal value, and
            may reject unrecognized values.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
          type: string
        kind:
          description: |-
            Kind is a string value representing the REST resource this object represents.
            Servers may infer this from the endpoint the client submits requests to.
            Cannot be updated.
            In CamelCase.
            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
          type: string
        metadata:
          type: object
        spec:
          properties:
            apiKeySecretRef:
              properties:
                key:
                  default: apiKey
                  type: string
                name:
                  minLength: 1
                  type: string
                namespace:
                  default: default
                  description: Namespace of the Secret in the same tenant workspace.
                    Defaults to default.
                  maxLength: 63
                  pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                  type: string
              required:
              - name
              type: object
            subscription:
              properties:
                id:
                  minLength: 1
                  type: string
                organizationID:
                  minLength: 1
                  type: string
                signingSecretRef:
                  properties:
                    key:
                      default: apiKey
                      type: string
                    name:
                      minLength: 1
                      type: string
                    namespace:
                      default: default
                      description: Namespace of the Secret in the same tenant workspace.
                        Defaults to default.
                      maxLength: 63
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                  required:
                  - name
                  type: object
              required:
              - id
              - organizationID
              - signingSecretRef
              type: object
            teams:
              description: Empty allows every team accessible to the key. Workspace
                administrators own this policy.
              items:
                properties:
                  id:
                    description: Linear team UUID, not its display key.
                    minLength: 1
                    type: string
                required:
                - id
                type: object
              maxItems: 100
              type: array
          required:
          - apiKeySecretRef
          type: object
        status:
          properties:
            checkedAt:
              format: date-time
              type: string
            message:
              type: string
            observedGeneration:
              format: int64
              type: integer
            ready:
              type: boolean
          required:
          - ready
          type: object
      required:
      - spec
      type: object
    served: true
    storage: true
    subresources:
      status: {}
