# Provider — declarative provisioning record (admin.faros.sh, admin-only).
# The hub's Provider controller creates root:faros:providers:agents, its
# "provider" ServiceAccount, and the agents-kubeconfig Secret in
# root:faros:system:providers. The APIExport/schemas/bind-grant come from the
# agents provider's own `init`; routing/UI from the companion CatalogEntry
# (Helm-rendered from deploy/chart/templates/catalogentry.yaml).
apiVersion: admin.faros.sh/v1alpha1
kind: Provider
metadata:
  name: agents
spec:
  displayName: "Agents"
