apiVersion: v1
kind: Service
metadata:
  name: {{ include "vibe-studio.fullname" . }}
  labels:
    {{- include "vibe-studio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vibe-studio.selectorLabels" . | nindent 4 }}
