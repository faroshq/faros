{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "vibe-studio.serviceAccountName" . }}
  labels:
    {{- include "vibe-studio.labels" . | nindent 4 }}
{{- end }}
