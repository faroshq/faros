apiVersion: v1
kind: Service
metadata:
  name: {{ include "deployments.fullname" . }}
  labels:
    {{- include "deployments.labels" . | nindent 4 }}
spec:
  selector:
    {{- include "deployments.labels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
