# Provider — declarative provisioning record (admin.railgrid.ai, admin-only).
# The hub's Provider controller creates root:railgrid:providers:edges, a "provider"
# ServiceAccount, and its kubeconfig Secret.
apiVersion: admin.railgrid.ai/v1alpha1
kind: Provider
metadata:
  name: edges
spec:
  displayName: "Edges"
