# Provider — declarative provisioning record (admin.faros.sh, admin-only).
# The hub's Provider controller creates root:faros:providers:edges, a "provider"
# ServiceAccount, and its kubeconfig Secret.
apiVersion: admin.faros.sh/v1alpha1
kind: Provider
metadata:
  name: edges
spec:
  displayName: "Edges"
