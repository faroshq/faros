apiVersion: apis.kcp.io/v1alpha1
kind: APIExport
metadata:
  name: workload.faros.sh
spec:
  permissionClaims:
  - group: "workload.kcp.io"
    resource: "synctargets"
    identityHash: ROOT_WORKLOAD_IDENTITY
    all: true
